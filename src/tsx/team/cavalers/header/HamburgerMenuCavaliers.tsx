import React, { useState } from "react";
import "./hamburgerMenuCavaliers.css";

const HamburgerMenuCavaliers: React.FC = () => {
  const [isOpen, setIsOpen] = useState(false);

  const toggleMenu = () => {
    setIsOpen(!isOpen);
  };

  return (
    <>
      <button
        className={`hamburgerButtonCavaliers ${isOpen ? "active" : ""}`}
        onClick={toggleMenu}
        aria-label="Toggle menu"
      >
        <span></span>
        <span></span>
        <span></span>
      </button>

      {/* Полный экран */}
      <div className={`fullscreenMenuCavaliers ${isOpen ? "open" : ""}`}>
        <nav className="menuContentCavaliers">
          <h1 className="menuContentHCavaliers">
            NBA Fansy League Flshng lights
          </h1>
          <a href="/" onClick={toggleMenu}>
            Главная
          </a>
        </nav>
      </div>
    </>
  );
};

export default HamburgerMenuCavaliers;
